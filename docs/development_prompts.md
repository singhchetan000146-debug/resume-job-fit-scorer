# Development prompt record

The application calls a local embedding model, not a generative LLM. It has no runtime chat prompt.

Actual user instruction for this implementation:

> make proper github assiment

This followed the user's supplied specification: Python with FastAPI or Flask, Pydantic request validation, an embedding or LLM call with error handling, PDF/TXT input, separate criterion scores and reasoning, configurable weights, three-sample calibration, unreadable resume handling, README and an honest one-page explanation.

The code was developed with AI assistance. For the walkthrough, show this actual development instruction and explain how it led to the implementation. Do not describe it as a runtime prompt or invent a past prompt. If the evaluator requires a runtime generative prompt specifically, clarify that their technical requirements also permit an embedding-only system.
